"use strict";
(() => {
    var _a;
    const batman = 'Batman';
    const Ironman = "Ironman";
    console.log(batman);
    console.log(((_a = batman[10]) === null || _a === void 0 ? void 0 : _a.toUpperCase()) || 'No esta presente');
})();
