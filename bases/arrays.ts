
(() => {

  const numbers: (number | string | boolean)[] = [12, 45, true, 'Cyborg'];

  const heroes = ['Raven', 'Beast boy', 'Robin', 'Starfire'];

  heroes.forEach(hero => console.log(hero.toUpperCase()));

})()